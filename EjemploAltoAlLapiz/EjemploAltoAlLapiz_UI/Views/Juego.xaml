<Page
    x:Class="EjemploAltoAlLapiz_UI.Views.Juego"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EjemploAltoAlLapiz_UI.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:EjemploAltoAlLapiz_UI.ViewModel"
    mc:Ignorable="d">

    <Page.DataContext>
        <vm:JuegoViewModel></vm:JuegoViewModel>
    </Page.DataContext>
    
    <RelativePanel >
        <RelativePanel.Background>
            <ImageBrush Stretch="Fill" ImageSource="Assets\img\pocketwatch.jpg"/>
        </RelativePanel.Background>

        <TextBlock Name="letraRandom" Text="{Binding Letra}" />
        <StackPanel Orientation="Horizontal">
            <TextBlock Name="txtBlock" Margin="30" Text="Categoria Uno" Foreground="White" FontSize="30" FontFamily="Assets/fuentes/Another day in Paradise.ttf#Another day in Paradise"/>
            <TextBox Name="txtBox"  Text="Categoria Uno" Height="50" Width="200" FontSize="30" FontFamily="Assets/fuentes/Another day in Paradise.ttf#Another day in Paradise" />
        </StackPanel>
        
        
        </RelativePanel>
</Page>
